{
  "name": "yaki-season-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "YAKI SEASON 단일 야키토리 주문 수직 슬라이스 — 웹 프로토타입",
  "scripts": {
    "dev": "python -m http.server 8777",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install chromium",
    "verify": "vitest run && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "ajv": "^8.20.0",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
