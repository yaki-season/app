// 기능 플래그.
//
// 손님 시스템(상태 머신·유형·다중 슬롯 재주문·인내심 게이지)은 구현돼 있지만
// 기본 비활성이다. 이유:
//
// - `epic/developer-1/005 (2.5D 영업 장면과 손님 반응)`이 범위에서 "복수 주문,
//   대기 게이지"를 명시적으로 제외하고, 구현 기준선을 수직 슬라이스 커밋으로
//   고정해 두었다. 손님 시스템이 켜져 있으면 그 기준선이 깨진다.
// - 손님의 정본 요구사항은 `spec/gameplay/GPL-003 주문·손님·좌석과 러시 운영`이며,
//   좌석·그룹·러시까지 포함한다. 현재 구현은 그보다 좁은 초기 설계 기준이라
//   재작업이 필요하다.
//
// 켜는 시점: `GPL-003` 기준으로 손님 모델을 재정렬하는 후속 작업에서.
// 그때까지 `src/state/customer/`와 그 단위 테스트는 휴면 상태로 유지한다.
export const CUSTOMER_SYSTEM_ENABLED = false;
