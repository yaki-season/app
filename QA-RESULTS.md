# 수직 슬라이스 검증 결과

- 검증일: `2026-07-21`
- 검증 기준: `QA-001 v1.0.0`, `GPL-001 v1.0.0`, `UI-001 v1.0.0`, `SYS-001 v1.0.0`, `ART-001 v1.0.0`
- 대상: 단일 네기마 주문의 조립·굽기·서빙 수직 슬라이스

## 재현 방법

```bash
npm install
npm run test:e2e:install   # 최초 1회
npm run verify             # 단위 + 종단 전체
```

개별 실행:

| 명령 | 대상 |
|---|---|
| `npm test` | 상태 전이·조리 판정 단위 테스트 |
| `npx playwright test qa-scenarios` | QA-001 시나리오 A~D와 캔버스 픽셀 검사 |
| `npx playwright test assets` | 에셋 404·콘솔 오류·보간·로딩 게이트 |
| `npx playwright test layout` | 최소 클릭 영역·화면 내 포함·겹침 |
| `npx playwright test capture` | 공정별 화면 캡처 생성 |

## 자동 검증

| 항목 | 결과 |
|---|---|
| 단위 테스트 | `32개 통과` |
| 종단 테스트 | `46개 통과` (`1280×720`, `1920×1080` 각 23개) |
| 반복 실행 | 전체 3회 연속 실행, 불안정·순서 의존 실패 `0건` |
| 에셋 404 | `0건` |
| 처리되지 않은 콘솔 오류 | `0건` |

조리 판정 경계는 `QA-001 §자동검증 2`가 지정한 값 그대로 검증한다.
실제 대기가 아니라 모의 시간을 주입한다.

| 경과 시간 | 기대 판정 | 결과 |
|---:|---|---|
| `2.499초` | 덜 익음 | 통과 |
| `2.500초` | 적정 | 통과 |
| `5.499초` | 적정 | 통과 |
| `5.500초` | 과다 | 통과 |
| `6.999초` | 과다 | 통과 |
| `7.000초` | 탄 상태 | 통과 |

## 수동 시나리오 A~D

`QA-001 §수동 검증 시나리오`의 단계를 `tests/e2e/qa-scenarios.spec.js`에 1:1로 옮겨
브라우저 자동화로 재현했다. **사람이 직접 조작한 결과가 아니라 스크립트 재현 결과다.**

| 시나리오 | 확인 내용 | 결과 |
|---|---|---|
| A. 정상 품질 서빙 | 주문표 순서, 조립 5단계, 그릴 배치, 앞면 적정 뒤집기, 뒷면 적정 회수, 접시·매트 서빙, 좋은 반응, 다시 하기 | 통과 |
| B. 잘못된 재료와 이른 클릭 | 대파 오입력 시 진행 미증가와 양쪽 강조, 이른 클릭 무시와 덜 익음 피드백, 이후 정상 완료 | 통과 |
| C. 탄 꼬치와 복구 | 7초 방치 시 실패 처리, 원인 표시, 다시 만들기 후 타이머·결과·선택 전부 초기화 | 통과 |
| D. 중단과 복귀 | 숨김 중 시계 정지, 7.5초 뒤 복귀해도 누적되지 않음, 같은 꼬치로 정상 서빙 | 통과 |

## 시각 검증

캡처 20장을 `captures/`에 생성했다 (`1280×720`, `1920×1080` × 공정 10단계).

| 항목 | 결과 |
|---|---|
| 주문표·작업 대상·상태 피드백·공정 탭 겹침 | 없음 (`layout.spec.js`가 경계 상자로 자동 검사) |
| 화면 밖 잘림 | 없음 (모든 조작 대상이 뷰포트 안) |
| 최소 클릭 영역 `44×44` | 충족 |
| 캔버스 빈 렌더·단색 오류 화면 | 없음 (픽셀을 직접 읽어 불투명 비율과 색 수 확인) |
| 닭·대파 구분 | 실루엣(둥근 사각 대 원형 단면), 질감, 색으로 구분 |
| 조리 5상태 구분 | 색 외에 그을음 면적, 진행 링 각도, 연기 농도로 구분 |
| 콘셉트 시트 문구·번호·커서 노출 | 없음 (에셋을 코드로 새로 생성) |

## 관찰 플레이테스트

**수행하지 못했다.** `QA-001 §관찰 플레이테스트`는 구현에 참여하지 않은 사용자 3명이
설명 없이 첫 주문을 시도하는 것을 요구하는데, 이 검증 환경에는 실제 사용자가 없다.

`QA-001 §예외 및 경계 조건`에 따라 자동·수동 기능 검증은 완료로 두고 관찰 검증은
미완료로 기록한다. 아래 지표는 전부 미측정이다.

| 지표 | 목표 | 결과 |
|---|---:|---|
| 첫 재료 선택까지 | `5초 이내` 2명 이상 | 미측정 |
| 첫 꼬치 조립 완료 | `30초 이내` 2명 이상 | 미측정 |
| 뒤집기 첫 시도 성공 | 3명 중 2명 이상 | 미측정 |
| 올바른 손님 서빙 성공 | 3명 중 3명 | 미측정 |
| 한 주문 뒤 조작 순서 설명 | 3명 중 2명 이상 | 미측정 |

손맛 4개 항목(`재료가 끼워지는 느낌`, `뒤집기 반응`, `적정 순간 인지`,
`접시 서빙의 무게감`)도 수집하지 못했다.

## 발견하고 수정한 결함

| 등급 | 내용 | 처리 |
|---|---|---|
| 차단 | 전역 입력 잠금이 서로 다른 대상의 연속 클릭을 삼켜 조립 완료 후 그릴 배치가 불가능했다 | 대상별 잠금으로 변경, 회귀 테스트 추가 |
| 높음 | `render()`가 주문표 DOM을 다시 만들어 오입력 강조가 즉시 지워졌다. `UI-001 §8`의 올바른 칸 강조가 동작하지 않았다 | 강조를 `render()` 뒤에 붙이도록 순서 수정, 시나리오 B로 고정 |
| 보통 | CSS `display: flex`가 `hidden` 속성을 덮어 굽는 중에도 대기 꼬치가 남았다 | `[hidden] { display: none !important }` 추가, 회귀 테스트 |
| 보통 | 결과 오버레이가 화면 전체를 덮어 손님 반응이 보이지 않았다 | 하단 패널로 분리, 손님 반응 표시를 테스트로 고정 |
| 보통 | 결과 오버레이가 반투명이라 뒤 화면 문구가 비쳤다 | 불투명 배경 |
| 보통 | 익힘 상태 배지가 진행 링과 겹쳤다 | 그릴 칸 바깥으로 이동, 겹침 검사 추가 |
| 보통 | 재료 비행 연출이 클릭 속도보다 느려 `조립 완료`인데 칸이 비어 보였다 | 연출을 380ms로 단축 |
| 낮음 | `1920×1080`에서 조작 대상이 화면 양끝으로 흩어졌다 | 중앙 정렬 |
| 낮음 | 배경의 1px 판자 선이 확대에서 뭉개졌다 | 배경을 큰 면 위주로 재작성 |
| 낮음 | 손님 3종 표정이 구분되지 않았다 | 얼굴 확대, 눈썹·눈·입 형태 분리, 땀방울 추가 |
| 낮음 | 꼬치 조각 경계가 붙어 하나의 기둥으로 보였다 | 조각별 1px 외곽선 |

검증 종료 시점 기준 **차단 등급 `0건`, 높음 등급 `0건`**이다.

## 남은 위험

1. **관찰 플레이테스트 미수행.** 클릭 이해도와 손맛은 검증되지 않았다. 자동 검증은
   "동작한다"만 증명하고 "이해된다"는 증명하지 못한다. 실제 사용자 3명 확보 후
   재검증이 필요하며, 이것이 가장 큰 미해결 위험이다.
2. **성능 미검증.** 헤드리스 환경에 GPU가 없어 종단 테스트는 SwiftShader 소프트웨어
   렌더링을 쓴다. `SYS-001`의 `1280×720` 평균 30 FPS 목표는 실제 GPU 브라우저에서
   따로 확인해야 한다.
3. **에셋 밀도.** 에셋은 코드로 생성한 픽셀 아트라 콘셉트 시트의 회화적 밀도에 못 미친다.
   기능과 상태 구분에는 문제가 없으나 최종 인상은 다르다.
4. **단일 브라우저.** Chromium에서만 검증했다. `SYS-001`이 최신 Chromium 계열만
   요구하므로 범위 내이지만, 다른 엔진은 미확인이다.
5. **배경 픽셀 밀도.** 배경은 `cover`로 늘어나 조작 대상 스프라이트와 배율이 다르다.
   가는 디테일을 배제해 이질감을 줄였으나 완전히 같지는 않다.

## 다음 검증 제안

1. 사용자 3명 관찰 플레이테스트를 먼저 수행한다. 특히 "그릴에 올린 뒤 언제 클릭해야
   하는지"를 설명 없이 알 수 있는지가 핵심 관찰 지점이다.
2. 실제 GPU 브라우저에서 조리 중 프레임 타임을 측정한다.
3. 적정 구간 진입 신호(진행 링 맞물림·윤기·연기)가 색각 이상 조건에서도 읽히는지
   확인한다. 현재 색 외 신호를 두 가지 넣었으나 실사용 검증은 없다.
