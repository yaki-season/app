# app 저장소 AI 작업 지침

## 저장소 역할

이 저장소는 게임 개발과 운영에 필요한 모든 구현 결과물을 관리한다.

- 애플리케이션 소스코드
- 테스트 코드
- 에셋 연동 코드
- 빌드와 배포 설정
- 운영 스크립트와 환경별 설정

요구사항, 기획 원본과 개발자별 작업 계획의 기준 문서는 별도 `docs` 저장소에서 관리한다.

## 작업 시작 규칙

1. 별도 `docs` 저장소의 `spec/`에서 관련 요구사항 상세 설계서를 확인한다.
2. `docs` 저장소의 `epic/`에서 담당 작업 상태가 `대기`, `진행 중` 또는 `변경 확인 필요`인지 확인한다.
3. 작업 상태를 `진행 중`으로 변경한 뒤 구현을 시작한다.
4. `inbox/` 자료만을 근거로 구현하지 않는다.
5. 요구사항과 작업 내용이 충돌하면 구현을 임의로 결정하지 않고 `docs` 저장소의 `inbox/99.change-request/`에 변경 요청을 기록한다.

## 구현 중 변경사항 처리

개선사항, 수정사항, 결함 또는 누락된 요구사항을 발견하면 다음 순서를 따른다.

1. `docs` 저장소의 `inbox/99.change-request/`에 변경사항을 기록한다.
2. `docs` 저장소의 `template/AI_변경사항_반영_파이프라인.md`에 따라 `spec/`과 `epic/`을 갱신한다.
3. 변경된 작업 상태가 `변경 확인 필요`이면 변경 범위와 재작업 영향을 확인한다.
4. 최신 `spec/`과 `epic/`을 기준으로 이 저장소의 구현을 계속한다.

## 완료 규칙

- 관련 작업의 완료 기준을 모두 검증한다.
- 구현 위치, 검증 방법, 결과와 남은 위험을 `docs` 저장소의 `epic/` 작업에 요약한다.
- 모든 기준을 충족한 뒤 작업 상태를 `완료`로 변경한다.

## 저장소 경계

- 구현·테스트·빌드·배포·운영 파일은 모두 `app`에 둔다.
- `docs`에는 소스코드나 실행 가능한 운영 파일을 복사하지 않는다.
- `app`에는 요구사항 문서를 중복 저장하지 않고 경로와 요구사항 식별자만 참조한다.
